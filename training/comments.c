#include <stdio.h>
// comments: here we will be working with comments (single line commment)
/* (multiline comments) this comment types can house comments beyond just one line */
/**
 * main - talking on comments (betty multi line comment smaple)
 * must contain comment's main function
 * Return: 0 must contain return value too 
*/
int main(void)
{
	printf("The classes of food for the day: "/* more information may be added later on */);
	int classes;
	scanf("%i", &classes);

	double rooms = (double) rooms / 12;

	printf("you have %f rooms classes.\n", rooms);

	return (0);

